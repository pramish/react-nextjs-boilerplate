import type { NextPage } from "next";
import { Layout } from "../src/common/layout";

const Home: NextPage = () => {
  return (
    <Layout title="React/NextJS Boilerplate | Home">
      <h1>This is a starting point of this boilerplate.</h1>
    </Layout>
  );
};

export default Home;
